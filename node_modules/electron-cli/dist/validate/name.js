'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = validateName;

var _validateNpmPackageName = require('validate-npm-package-name');

var _validateNpmPackageName2 = _interopRequireDefault(_validateNpmPackageName);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function validateName(name) {
  var _validate = (0, _validateNpmPackageName2.default)(name),
      errors = _validate.errors,
      warnings = _validate.warnings;

  var error = '';

  if (errors) {
    error = errors.join(',');
  }

  if (warnings) {
    error = warnings.join(',');
  }

  if (!errors && !warnings) {
    return true;
  }

  return error;
}